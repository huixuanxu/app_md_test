import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    starlist: {
        flexDirection: "row"
    },
    stars: {
        marginRight: 4,
        width: 14,
        height: 14
    }
})

export default styles;